import React from 'react';
import {Footer} from './Footer';

export const FooterComponent = () => {
    return (
        <div style={{paddingTop: 30}}>
            <Footer />
        </div>
    );
};

export default {
    title: 'Footer',
};
